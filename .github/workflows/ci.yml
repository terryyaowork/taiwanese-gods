name: CI/CD

on:
  push:
    branches: [main]
  pull_request:
    branches: [main]

jobs:
  build:
    name: 建置與檢查
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4

      - name: 設定 pnpm
        uses: pnpm/action-setup@v4
        with:
          version: 9

      - name: 設定 Node.js
        uses: actions/setup-node@v4
        with:
          node-version: 22
          cache: pnpm

      - name: 安裝依賴
        run: pnpm install --frozen-lockfile

      # 三語資料一致性（筆數 / id 順序 / 欄位齊全）+ 文件數字對帳
      # 擋的是「某個語系少一段內容」與「文件數字過期」這兩類靜默錯誤 — build 不會發現
      - name: 一致性對帳
        run: pnpm run verify

      - name: Astro 語法檢查
        run: pnpm run check

      - name: 建置網站
        run: pnpm run build

      - name: 上傳建置產物
        uses: actions/upload-artifact@v4
        with:
          name: dist
          path: dist/
          retention-days: 7

  deploy:
    name: 部署至 Cloudflare Pages
    needs: build
    if: github.ref == 'refs/heads/main' && github.event_name == 'push'
    runs-on: ubuntu-latest
    steps:
      - name: 下載建置產物
        uses: actions/download-artifact@v4
        with:
          name: dist
          path: dist/

      - name: Deploy to Cloudflare Pages
        uses: cloudflare/wrangler-action@v3
        with:
          apiToken: ${{ secrets.CLOUDFLARE_API_TOKEN }}
          accountId: ${{ secrets.CLOUDFLARE_ACCOUNT_ID }}
          command: pages deploy dist --project-name=taiwanese-gods --branch=main --commit-dirty=true
