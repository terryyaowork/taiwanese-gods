---
import temples from '~/data/zh-TW/temples-east.json';

export async function getStaticPaths() {
  return temples.map((temple) => ({
    params: { id: temple.id },
  }));
}

const { id } = Astro.params;
return Astro.redirect(`/temples/${id}`);
---
