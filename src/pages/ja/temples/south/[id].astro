---
import temples from '~/data/ja/temples-south.json';

export async function getStaticPaths() {
  return temples.map((temple) => ({
    params: { id: temple.id },
  }));
}

const { id } = Astro.params;
return Astro.redirect(`/ja/temples/${id}`);
---
