---
import temples from '~/data/en/temples-central.json';

export async function getStaticPaths() {
  return temples.map((temple) => ({
    params: { id: temple.id },
  }));
}

const { id } = Astro.params;
return Astro.redirect(`/en/temples/${id}`);
---
