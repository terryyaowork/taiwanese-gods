# 產品功能進度台帳

> 建立 2026-08-09。**每次要動這個站之前先讀這份**，收工前更新這份。
>
> 這個站是來來回回蓋的（一次一小塊、隔幾天甚至幾週再回來），所以需要一個「上次做到哪、下一步從哪接、有哪些坑已經知道」的固定入口。
>
> - 要蓋什麼 / 為什麼是這個順序 → `ROADMAP.md`
> - 內容故事化進度（地基層）→ `CONTENT-PROGRESS.md`
> - 怎麼寫 → `CONTENT-STYLE.md`

## 開工 SOP（每次 session 前三步）

1. 讀本檔的「下一步」+「已知坑」兩段
2. 決定這次只做**一件事**（一個功能 / 一條路線 / 一個資料層改造），不要開多線
3. 動手前先講清楚**驗收怎麼算完成**，講不出來就是還沒想清楚 — 回頭想，不硬做

收工時：更新「下一步」+ 台帳狀態 + 在「變更紀錄」加一行。

> 文件裡的數字（build 頁數、覆蓋率、筆數、流量）都會過期。**看到數字先確認日期**，
> 流量一律看週報 state file，其餘用下方「驗收清單」的指令當場重算，不要照抄。

## 👉 下一步

**⏳ 等 GA4 的 `?from=fortune` 數據累積 2–3 週**（Step 0 已於 2026-08-09 上線）。
看 GA4 頁面報表中帶 `?from=fortune` 的 `/temples/*`、`/routes/*` 有多少次——
**用那個數字決定要不要啟動 ②祈福（線上點燈 A 版）**，理由見 `ROADMAP.md`。

期間可平行做、與數據不相依的候選：

1. **補廟宇資料庫**（熱門神明查無主祀廟，清單見下方「順帶發現」）— 同時提升 CTA 覆蓋率
2. **`/guide` 也接上同一個 CTA**（推薦邏輯已抽成 `temple-recommendations.ts`，套上去很便宜）

---

## ✅ Step 0 已完成並上線（2026-08-09）

**求籤結果 → 推薦真廟 / 路線**，三語同步上線。

- 推薦邏輯：用戶求籤時選的那尊神 → 祂的主祀廟（最多 3 間、依 sortOrder）→ 有經過那些廟的路線（最多 2 條）
- 實作：`src/utils/temple-recommendations.ts`（共用）+ 三份 `fortune.astro` 的 CTA 區塊與 `ctaLabels`
- fallback：查無主祀廟 → 導 `/routes`；原民信仰 → 完全不顯示（聖地是山與部落祭祀場，不是廟）
- 量測：所有 CTA 連結帶 `?from=fortune`
- 驗收：`npm run check` 0 errors ／ `npx playwright test` 21 passed（新增 5 個 CTA 測試）／ build 736 頁不減 ／
  三語 + 手機寬度截圖確認（EN 廟名較長，已改成手機直排、桌機同列）
- **線上複驗（2026-08-09，CI + Cloudflare Pages 部署後）**：三語 CTA 皆顯示且連結帶 `?from=fortune`、
  三太子 fallback 導 `/routes`、原民（tayal-utux）整塊不顯示 — 全部符合設計

> ⚠ 驗線上頁面時 curl GET 這個站在本機環境會失敗（HEAD 通、GET exit 43），
> 用 Playwright 開真實網址驗最可靠（順便驗到 client-side 渲染的結果）。注意路徑要帶尾斜線，否則吃 308。

### ✅ 資料鏈已驗證可通（2026-08-09 實測）

```
神明 g.id  →  temples[].mainDeities[].id   →  routes[].stops[].templeId
```

`temples-*.json` 的 `mainDeities` 是 `[{id, name}]`——**已經帶神明 id**，所以反查是精準比對，
**不需要**做「廟名字串 → temple id」的映射（原本以為的最大障礙，見下方缺口 #1 更正）。

**覆蓋率實測：28 / 53 尊神明查得到廟（53%）**，而且求籤最常用的主要神明覆蓋很好：
媽祖 14 間、月老 5、關聖帝君 5、城隍爺 5、觀世音 5、土地公 3、文昌帝君 2、玉皇大帝 2。

### Step 0 的三個設計決策（實作時真正花心思的地方）

1. **fallback**：25 尊查無主祀廟。其中原民 7 尊 + 矮靈祭的「聖地」是山與部落祭祀場（大武山 / 玉山 / 蘭嶼），
   **本來就不該導去廟宇頁**；地基主 / 門神 / 土地婆 / 七娘媽 / 太歲星君 是概念性神明，沒有專屬廟很正常。
   → 已實作：查無廟時**不顯示壞掉的 CTA**，改導 `/routes`；`category === 'indigenous'` 整塊不顯示。
2. **三語 CTA 文案**：屬服務包裝層 → 適用 `CONTENT-STYLE.md` 的發布 gate（上線前用戶過目）。
   語氣照路線設計哲學走「安靜獨行」：推平日早晨、人少，不推打卡熱點。
3. **量測（最省版）**：CTA 連結加 query 參數（`/temples/xxx?from=fortune`），
   GA4 頁面報表直接看得到點擊數，**零 JS、零埋點**。不做事件體系、不追加分析建設。
   這個數字是「②祈福要不要做」的唯一判準，所以跟 Step 0 一起上，不事後補。

### 順帶發現：廟宇資料庫的真缺口

以下是熱門神明卻在 57 間廟宇資料庫裡查無主祀廟——補廟比做字串映射有價值：

- **清水祖師**：三峽長福巖、艋舺清水巖、淡水清水巖（三間都是知名廟）
- **客家神明**：五穀神農（苗栗獅潭神農宮 / 新竹北埔五穀宮）、定光古佛（淡水鄞山寺）、慚愧祖師（南投鹿谷鳳凰山寺）
- **其他**：三太子、註生娘娘、地藏王（艋舺地藏庵）、觀音（林口竹林山觀音寺 / 清水紫雲巖 / 高雄內門紫竹寺）

## 服務層功能台帳

圖例：✅ 已上線｜🔄 進行中｜⬜ 待建｜⏳ 等前置數據｜❌ 已拿掉

| 功能                         | 狀態 | zh  | en  | ja  | 檔案                                                                       | 缺口 / 備註                                                                       |
| ---------------------------- | ---- | --- | --- | --- | -------------------------------------------------------------------------- | --------------------------------------------------------------------------------- |
| 線上求籤（60 籤）            | ✅   | ✅  | ✅  | ✅  | `src/pages/{,en/,ja/}fortune.astro` + `components/common/Divination.astro` | 結果頁已接上真廟 / 路線 CTA（Step 0）                                             |
| 擲筊                         | ✅   | ✅  | ✅  | ✅  | 同上（Divination.astro，含擲筊確認流程）                                   | —                                                                                 |
| 「該拜哪尊神」導引           | ✅   | ✅  | ✅  | ✅  | `src/pages/{,en/,ja/}guide.astro` + `data/*/god-guide.json`（7 類）        | **仍沒有導去廟宇 / 路線** — 同一個 CTA 套上去即可，下一個便宜的漏斗點             |
| 求完 → 推薦真廟 / 路線       | ✅   | ✅  | ✅  | ✅  | `src/utils/temple-recommendations.ts` + 三份 `fortune.astro`               | **Step 0 已上線**（2026-08-09，線上複驗過）；連結帶 `?from=fortune`               |
| 每日運勢                     | ⬜   | —   | —   | —   | 待建                                                                       | 彈藥現成（60 籤）；定位是回訪不是 SEO                                             |
| 拜拜新手指南 / 供品禁忌      | ⬜   | —   | —   | —   | 待建                                                                       | 部分素材已在 `culture-rituals.json`（8 條）                                       |
| 黃曆宜忌                     | ⬜   | —   | —   | —   | 待建                                                                       | 需外部曆法資料來源，尚未評估                                                      |
| 神明生日 / 節慶行事曆        | ⬜   | —   | —   | —   | 待建                                                                       | 卡在資料層 #3（生日沒有結構化欄位）                                               |
| 線上點燈 / 安太歲（A 版）    | ⏳   | —   | —   | —   | 待建                                                                       | 無狀態 localStorage、零後端零個資；**等 CTA 點擊數據再啟動**                      |
| 補廟宇資料庫（熱門神明缺廟） | ⬜   | —   | —   | —   | `data/*/temples-*.json`                                                    | 候選項目；清單見上方「順帶發現」                                                  |
| 參拜紀錄 / 打卡              | ⬜   | —   | —   | —   | 待建                                                                       | 🔴 需後端狀態                                                                     |
| ~~許願牆 / 祈願區~~          | ❌   | —   | —   | —   | —                                                                          | **2026-08-09 拿掉**，理由見 `ROADMAP.md` 決策紀錄；要翻案先回答「誰會用、為什麼」 |

## 地基層（內容）現況

全部三語上線、零語系缺口。逐項看 `CONTENT-PROGRESS.md`。

| 內容     | 筆數 | 備註                                                              |
| -------- | ---- | ----------------------------------------------------------------- |
| 神明     | 46   | 道教 15 / 佛教 11 / 民間 13 / 客家 7                              |
| 原民信仰 | 7    |                                                                   |
| 廟宇     | 57   | 北 21 / 中 12 / 南 13 / 東 11                                     |
| 文化     | 44   | 習俗 10 / 節慶 9 / 工藝 7 / 儀式 8 / 咒語 10                      |
| 神明故事 | 10   | 700–800 字（原目標 800–1500，實際偏短，可再加長）                 |
| 路線     | 8    | **8/8 皆已配廟口美食**                                            |
| 籤詩     | 60   | 三語解說齊全                                                      |
| 神明指南 | 7    | academic / wealth / love / health / fertility / justice / removal |

**內容層剩餘小尾巴**：北港朝天宮老街美食（目前無對應中部路線，待有路線再補）。

### ✅ 三語欄位缺口 13 筆已補（2026-08-09）

`npm run verify` 首次執行就抓到 13 筆隱形缺口——過去宣稱的「三語零缺口」只驗過**筆數 / id / 順序**，
沒驗**欄位齊全**。`GodDetail.astro` 對 `worship` / `festivals` 是條件渲染（`god.worship &&`），
欄位缺 = 該語系頁面整段消失，是內容缺頁不是排版問題。

補完的內容（21 個欄位、以 zh 原文約 3,739 字改寫）：

| 語系 | 神明                                             | 補的欄位            |
| ---- | ------------------------------------------------ | ------------------- |
| en   | 孔子                                             | festivals           |
| ja   | 觀世音菩薩 / 阿彌陀佛 / 彌勒佛 / 文殊菩薩        | festivals           |
| ja   | 三太子 / 註生娘娘 / 保生大帝                     | worship + festivals |
| ja   | 三山國王 / 義民爺 / 伯公 / 慚愧祖師（客家 4 尊） | worship + festivals |
| ja   | 阿立祖                                           | worship + festivals |

寫法：以 zh 為準改寫非逐字直譯，ja 用です・ます體、文化名詞首次出現給讀音；
日期 / 供品 / 地名 / 數字一律照原文，不發明。**`npm run verify` 已納入 CI**，同類缺口不會再靜默發生。

## ⚠ 已知坑 / 資料層缺口

做 CTA 或行事曆之前一定會撞到這幾個：

1. ~~**神明的 `associatedTemples` 是純字串陣列**，不是 templeId~~
   **2026-08-09 更正：這條有繞道，不再是阻礙。** `associatedTemples` 確實是純字串（`"松山慈祐宮 (台北市松山區)"`，83 筆裡只有 58% 能精準對到廟宇資料庫），
   但**不需要用它**——`temples[].mainDeities` 已經帶神明 id，反查即可（見上方「資料鏈已驗證可通」）。
   `associatedTemples` 維持現狀當展示文字用，不投資去做映射。
2. **`fortunes.json` 沒有任何神明 / 廟宇關聯欄位**
   欄位只有 `id / ganzhi / quality / poem / explanation / keywords`。所以「求完推薦廟」的關聯必須從**用戶選的神明**那邊來，不是從籤詩來。
3. **神明生日沒有結構化欄位**
   `festivals` 是一段敘述文字（例：「農曆三月二十三日『媽祖聖誕』…」）。要做行事曆得先抽出結構化日期（農曆 / 國曆）。
4. **量測刻意保持很輕**：GA4 只有 pageview（`src/config.yaml` G-HR8L9Z4Y4E），沒有互動 event，**這是決定、不是缺口**。
   只有 CTA 點擊靠 `?from=fortune` query 參數量（零 JS）。求籤完成率 / 擲筊次數暫不量——好奇但不影響任何決策。
   週報 pipeline 抓的是 users/sessions + GSC clicks/impressions。
5. **多 agent 同檔 append 有 race 疑慮**：曾讓多個 agent 並行 append 同一個 JSON。下次同檔多筆寫入走「各寫獨立暫存檔、主線合併」。
6. ~~**`verify-consistency.mjs` 已遺失**~~ **2026-08-09 重建：`npm run verify`**（`scripts/verify-consistency.mjs`）。
   一次驗兩件事：三語資料一致性（筆數 / id 順序 / **欄位齊全**）＋ 文件數字對帳（筆數表、覆蓋率、路線美食）。
   不一致就 exit 1。**動完資料或改完文件都跑一次**——文件裡的數字是負債，靠人記得改不可靠。
7. **`astro build` 過 ≠ `astro check` 過**：build 不做 TS 檢查。push 前一律 `npm run check`（astro + eslint + prettier 三關），CI 是 `pnpm run check`。
8. **資料裡的 `**` 會字面顯示**：`GodDetail.astro`是`text.split('\n').map(line => <p>{line}</p>)`，
**不解析 markdown**。目前有 **78 個欄位**含 `**`（zh 23 / en 11 / ja 44），線上實際看得到裸露的星號
（例：`/gods/taoism/confucius/` 的聖誕千秋段）。**寫新內容不要用 `**`。\*\*
   ⏸ 既有 78 筆怎麼處理待決定：(a) 從資料移除記號 (b) 讓 GodDetail 支援粗體渲染（保留原意，一處改動全語系受益，建議）。

## 驗收清單（動完必跑）

```bash
npm run verify         # 三語一致性（筆數 / id 順序 / 欄位齊全）+ 文件數字對帳 — 已在 CI，會擋 push
npm run check          # 三關：astro check + eslint + prettier — push 前必過
npx astro build        # 頁數不減（2026-07-05 基準 736 頁）
npx playwright test    # tests/fortune.spec.ts — 動到求籤 / 擲筊必跑
```

互動功能額外驗收：**三語都手動點過一輪**（不是只看 zh），且 CTA / 結果流程在手機寬度不破版。

## ❌ 已決定不做（別再重新辯論）

| 項目            | 決定日     | 為什麼                                                                                                                                               | 翻案條件                       |
| --------------- | ---------- | ---------------------------------------------------------------------------------------------------------------------------------------------------- | ------------------------------ |
| 許願牆 / 祈願區 | 2026-08-09 | UGC 會破壞好不容易建立的聲音、審核成本是永久的、跟「安靜獨行」氣質不合；流量也還撐不起（W21 單週 702 users，空牆比沒牆更傷，但**這條會隨流量過期**） | 先回答「誰會用、為什麼」       |
| 精細事件追蹤    | 2026-08-09 | 只有 CTA 點擊會影響決策，其餘量了也不會改變任何事                                                                                                    | 有一個決策真的卡在某個數字上時 |
| 廟名字串映射層  | 2026-08-09 | `mainDeities` 已帶神明 id，反查即可，不需要映射                                                                                                      | —（技術上已無必要）            |

## 變更紀錄

| 日期       | 動了什麼                                                                                                                                |
| ---------- | --------------------------------------------------------------------------------------------------------------------------------------- |
| 2026-08-09 | 補齊 13 筆三語欄位缺口（21 個欄位）；`npm run verify` 納入 CI；順帶發現資料裡 78 個欄位的 `**` 會字面顯示（待決定怎麼處理）             |
| 2026-08-09 | 建 `npm run verify` 一致性對帳腳本（三語資料 + 文件數字），首跑即抓到上述 13 筆隱形缺口                                                 |
| 2026-08-09 | **Step 0 上線**：求籤結果 → 真廟 / 路線 CTA 三語（commit `1a61ec8`）；新增 5 個 playwright 測試；CI + Cloudflare 部署成功、線上複驗通過 |
| 2026-08-09 | 決策：三語一律同步（鐵律）、②祈福採 A 版且等 CTA 數據、許願牆拿掉、量測只留 CTA query 參數                                              |
| 2026-08-09 | 實測資料鏈可通（`mainDeities` 帶神明 id、覆蓋 28/53），推翻原本「需做廟名映射」的判斷                                                   |
| 2026-08-09 | 建立本台帳；roadmap 對帳修正（①問神實際已上線求籤/擲筊/導引、執行順序改成 CTA 優先）                                                    |
| 2026-08-08 | 定位轉軸：雲端廟宇互動服務層（`ROADMAP.md` + `CONTENT-STYLE.md` 聲音層）                                                                |
| 2026-07-05 | 全站三語故事化完成；路線 8 條 + 廟口美食；日文籤詩補齊 60 首                                                                            |
| 2026-07-04 | 路線 section 0→1；導覽加「主題路線」入口                                                                                                |
